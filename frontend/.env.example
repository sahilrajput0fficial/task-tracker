# TaskTrack Frontend Environment Variables
# In development, leave empty to use Vite's dev proxy (prevents cross-origin cookie issues)
# In production, set to your backend API URL (e.g. https://api.tasktrack.com)
VITE_API_URL=
