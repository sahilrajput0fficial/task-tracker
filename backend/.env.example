# Application Settings
PROJECT_NAME="Task Tracker API"
VERSION="1.0.0"

# JWT & Security Configuration
# IMPORTANT: Generate a secure random key for production (e.g. using `openssl rand -hex 32`)
SECRET_KEY="replace_with_a_secure_random_secret_key_minimum_32_characters_long"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

DATABASE_URL="sqlite:///./task_tracker.db"
